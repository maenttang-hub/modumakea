name: Validation Baseline and Nightly Extended

on:
  pull_request:
  push:
    branches:
      - main
      - master
  schedule:
    - cron: '0 18 * * 0'

jobs:
  baseline:
    name: Baseline Reliability Gate (public subset)
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Build
        run: npm run build

      - name: Full test suite
        run: npm test

      - name: Install Playwright browsers
        run: npx playwright install --with-deps chromium

      - name: Browser smoke tests
        run: npm run test:e2e

      - name: Run baseline validation gate
        run: npm run test:validation:baseline

  extended:
    name: Extended Validation Nightly (local-heavy / broader coverage)
    if: github.event_name == 'schedule'
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Run extended validation suite
        run: npm run test:validation:extended
